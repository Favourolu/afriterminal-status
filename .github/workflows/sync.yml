name: Sync Dashboard

on:
  schedule:
    # Runs every 15 minutes — picks up scraper runs within one refresh cycle
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout status repo
        uses: actions/checkout@v4

      - name: Fetch dashboard from main repo
        id: fetch
        run: |
          if curl -sSf \
            "https://raw.githubusercontent.com/Favourolu/afriterminal/main/ops_dashboard_index.html" \
            -o index_raw.html; then
            echo "ok=true" >> "$GITHUB_OUTPUT"
          else
            echo "Source file not yet on main — skipping sync"
            echo "ok=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Patch with status-site additions
        if: steps.fetch.outputs.ok == 'true'
        run: python3 patch.py index_raw.html > index_new.html

      - name: Check for changes
        if: steps.fetch.outputs.ok == 'true'
        id: diff
        run: |
          if diff -q index.html index_new.html > /dev/null 2>&1; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit updated dashboard
        if: steps.fetch.outputs.ok == 'true' && steps.diff.outputs.changed == 'true'
        run: |
          cp index_new.html index.html
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "chore: sync dashboard from main repo [skip ci]"
          git push
